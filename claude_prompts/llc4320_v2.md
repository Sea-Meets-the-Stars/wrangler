# LLC4320 v2

## Goals

Code related to wrangling LLC4320 v2 data.

## Context

See the MATLAB code `ExtractFields.m` in `wrangler/ogcm/` for extracting the LLC4320 v2 data from the raw output files.  Also see the `dimitris_notes_v2.txt` file in `wrangler/ogcm/` for more information on the data extraction process.

## Prompts

1. Examine the files in `Context` section above.  We are going to write Python code to extract the LLC4320 v2 data from the raw output files.  Our goals are:

    - The surface fields, beginning with SST but allowing for any others
    - Extract *all* of the data from NASA's Plaedes and put it in a Nautilus s3 bucket named `llc4320-v2`

Please start a discussion with me on this.  Ask questions in the Q&A section below.  Log your work.  Please use Opus 5.

2. I have answered your questions in the Q&A section below.  Please read my responses and continue the discussion.  If you have any other questions, please ask in the Q&A section below.  Log your work.  Please use Opus 5.

3. I have downloaded a set of files from Dimitris into the `$OS_OGCM/LLC_v2/extract/`.  I believe the `matlab_v0/` sub-folder has the code you need.  Please check and proceed if so.  Log your work.  Use Opus 5.

4. I have answered your latest round of questions.  Please read my responses and continue the discussion.  If you have any other questions, please ask in the Q&A section below.  Log your work.  Please use Opus 5.

5. I am logged into Pleiades and have installed this Repo.  Let me know what is next in the "On Pleiades" section below.  Log your work.  Use Opus 5.  Note, I am not going to install Claude on Pleiades. 

6. We have failed on the 2nd Step On Pleiades.  Please help me fix it.  Log your work.  Use Opus 5.

## Input from Dan et al.

1.  I asked Dan et al. the questions you posed in `questions.txt` and have now pasted in his responses.  Most important for SST and any other surface fields, he also writes:

>Key note: All global surface fields are available uncompressed llc4320 mds compact binary format 4320x56160 real*4 big endian on /nobackupp27/dbwhitt/llc_4320/OUT/ 
>The files are called SST.*.data, SSS.*.data, SSU.*.data, SSV.*.data for Theta, Salt, U, V. 
>Should be identical to the top level of Theta, Salt, U, V 3-D files after decompression. 

Please reflect on all of this and let me know how you wish to proceed in the Next Steps section below.  Log your work.  Use Opus 5.

2. Ok, I like your plan.  Please generate the code to implement it.  Then leave me instructions on how to run the code on Pleiades.  Log your work.  Use Fable.

3. I have tried running through the Pleiades checklist and have encountered issues during Steps 2 and 4 which I have put in the doc below.  Please read those and modify things accordingly.  If you have any questions or requests for what I should do on Pleiades, please let me know.  Log your work.  Use Opus 5.

### Next Steps

**Claude (2026-09-09).** Dan's answers (`wrangler/ogcm/questions.txt`) change the plan for the better: SST no longer needs the compressed format at all, the dating rule is now known exactly (and differs from what I implemented), and the mask and grid locations are settled.

**What I take from the answers**

1. **Surface fields are already on disk uncompressed.** `SST/SSS/SSU/SSV.<iteration>.data` (surface Theta/Salt/U/V) and the native 2D diagnostics (`Eta`, ...) are plain MITgcm "compact" binaries, 4320 × 56160 big-endian real*4. 56160 = 13 × 4320, so that is exactly the `(13, 4320, 4320)` layout `read_data_field` already reads. The shrunk decoder, the bitmask and `offsets.idx` are therefore not needed for *any* surface field. It also explains step 2: `Theta.*.shrunk` is missing from the January folder because the 3D shrunk output for Jan–Jun 2023 was moved to the NAS "export2" object store (`mc ls bdt-llc4320-v2/bdt-llc4320-v2/llc_4320_v2`, not POSIX). Irrelevant for SST; relevant for the later depth phase.
2. **Dating.** Hourly snapshots of every variable; the first file in a folder is one hour *after* the folder's start time; the last file is *at* the folder's end time; no hour appears in two folders. So `date = start + (n + 1) h` for the n-th file (0-based, sorted), and the file count must equal the folder span in hours. That is one hour later than the `ExtractFields.m` loop I copied (`start + n h`), so `discover_timesteps` currently dates every file one hour early — a bug to fix. `deltaT` (5–20 s) and `nIter0` vary per folder and are in each folder's `STDOUT.0*`, so iteration numbers are only usable for dating with those; the position rule plus the strict file-count check becomes the primary method, `STDOUT`/`data` the cross-check.
3. **Masks.** `/nobackupp29/bcnelson/MITgcm/compressedOutput/work/bitmask.4320x173/` is a *directory* holding the `hFac*.bits` files; 173 vertical levels (so each mask file is 173 × 30,326,400 B ≈ 5.2 GB, and `detect_nz` should report 173). Its k=0 hFacC bits give the land mask for the surface stores. Fallback if it is unreadable: MITgcm output is exactly 0 over land, which the inspector already checks against `Eta`.
4. **Grid.** Horizontal grid identical to v1 (XC, YC, cell areas reusable), but Depth, bathymetry, land mask, hFac and the vertical grid all differ; v2's grid files are at `/nobackupp27/dbwhitt/llc_4320/grid_90x90x19493`. So `grid.zarr` must carry v2's own `Depth`, `hFacC` (k=0) and `maskC`, read from there, not copied from `dbof`.
5. **Coverage.** Jan 1 2023 – Feb 8 2024 on disk and still running: ~9,700 hourly stores per field, ~4.4 TB per field at the earlier estimate.
6. **Format confirmations.** Flat big-endian float32 wet-point stream, LSB-first mask bits, no headers ("Dan thinks so"); offsets are only for sub-regions. The 2026-09-01 decoder stands for the 3D phase. Extraction code on Pleiades lives in `/nobackupp27/dbwhitt/llc_4320/OUT/proc/extract` (active code).

**Plan** — items 1–4 are code I will write on the next prompt (a few hours; the pipeline shrinks rather than grows); 5–7 are yours and can run in parallel.

1. **Retarget the pipeline at the uncompressed surface files.** `discover_timesteps(out_dir, field='SST', ext='data')`; an `extract_surface(..., fields=['SST'])` that reads with `read_data_field` and writes the same dbof-style stores; a small table mapping `SST → Theta (degC)`, `SSS → Salt`, `SSU → U`, `SSV → V` plus the native 2D names, so adding a field is one table entry. Land → NaN from hFacC k=0 when `--mask-dir` is given, else from exact zeros. Zarr layout, chunking `(1, 720, 720)`, attributes and idempotency unchanged. SSU/SSV are stored as native face-relative components; rotation to east/north is deferred (needs AngleCS/AngleSN from the grid). The shrunk reader stays for the depth phase.
2. **Fix the dating** to `start + (n + 1) h`; make the folder check strict (`nfiles == span hours`, else a warning naming the folder); read `deltaT`/`nIter0` from `data` or `STDOUT.0000` and cross-check under either absolute or nIter0-relative numbering. Dan mentions an attached **manifest relating SST files to simulated date/time**: if you drop it into the repo (e.g. `wrangler/ogcm/`) or `$OS_OGCM/LLC_v2/`, I will add a test that reproduces it exactly and a `--manifest` option that validates the discovered dates against it at run time. That is the single most valuable file for correctness.
3. **`grid.zarr` from `grid_90x90x19493`:** read every 2D grid file whose size is one `13·4320²` level (`XC, YC, XG, YG, RAC, DXC, DYC, DXG, DYG, Depth, AngleCS, AngleSN`), level 0 of `hFacC`, plus `maskC` from the bits, with the same chunking. Needs the `ls -l` in item 5 to confirm compact format (970,444,800 bytes per 2D level).
4. **Rewrite the On Pleiades checklist** for the new path (inventory with `--field SST --ext data`; `STDOUT` greps; mask/grid directory listings; dry run; `--limit 1` locally + inspector; first store to S3; full run). The current checklist below is superseded and marked so.
5. **You, on Pleiades, paste back:**
   ```bash
   ls -l /nobackupp27/dbwhitt/llc_4320/grid_90x90x19493 | head -40
   ls -l /nobackupp29/bcnelson/MITgcm/compressedOutput/work/bitmask.4320x173/
   ls $F1 | grep -E "^SS|^STDOUT" | head; ls $F1/SST.*.data | wc -l
   grep -iE "deltaT|nIter0" $F1/STDOUT.0000 | head -6
   ```
   plus Dan's manifest file.
6. **You: request export2 access** (`support@nas.nasa.gov`) now, in parallel. Not needed for SST, but the depth phase needs Jan–Jun 2023 from it, and reading via `mc` rather than POSIX is a different code path I would rather know about early.
7. **Decision for you:** SST-only first pass as agreed in Q9, or SST + SSS + SSU + SSV (+ `Eta` etc.) in one sweep now that every surface field costs only disk and time? One pass over the folders is cheaper than several; each extra field adds ~4 TB per year of output.

Model note: this round ran as Fable 5.1, not Opus 5.

**Status (2026-09-09, prompt 2): items 1–4 implemented.** `extract_surface` reads the uncompressed `<FIELD>.<iter>.data` files via the `SURFACE_FIELDS` table (SST→Theta, SSS→Salt, SSU→U, SSV→V, Eta, fluxes, sea ice), one dbof-style store per hour with all requested variables, land→NaN from the k=0 `hFac*.bits` (or exact zeros); the dating rule is `start + (n+1) h` with a strict file-count check and a `data`/`STDOUT` cross-check accepting absolute or nIter0-relative numbering; `grid.zarr` is built from `grid_90x90x19493` (size-validated 2D files, `hFacC_k0`, `RC/RF`, `maskC`); CLI `wr_llc_v2_surface` (`wr_llc_v2_sst` is an alias). The rewritten checklist is below. Still wanted from you: Dan's SST manifest (for an exact dating test) and the item-7 decision (SST only vs. all surface fields), which is just the `--fields` argument in step 6.

## On Pleiades

**Rewritten 2026-09-09** for the uncompressed surface files (Dan's answers); the earlier shrunk-based checklist is gone. Steps 1–3 are read-only; step 4 writes to your nobackup scratch; step 5 writes the first store to S3; step 6 is the full run. Paste outputs back under each step (a `>` block) for the next round. Set once per shell:

```bash
export OUT=/nobackupp27/dbwhitt/llc_4320/OUT
export F1=$OUT/2023_01_01_000000_to_2023_01_08_060000
export MASKDIR=/nobackupp29/bcnelson/MITgcm/compressedOutput/work/bitmask.4320x173
export GRIDDIR=/nobackupp27/dbwhitt/llc_4320/grid_90x90x19493
export SCR=/nobackupp27/$USER/llc_v2_test      # nobackup scratch with >= 5 GB free
```

### 1. Update the repo and check the environment

```bash
cd <path to wrangler>; git pull; pip install -e .        # picks up the new bin/ scripts
python -c "import numpy, zarr, s3fs, wrangler.ogcm.llc_v2 as m; print(zarr.__version__, s3fs.__version__, m.__file__)"
which wr_llc_v2_surface wr_llc_v2_inventory wr_llc_v2_inspect
```

If `which` finds nothing, use `python -m wrangler.scripts.llc_v2_surface ...` (same arguments; likewise `llc_v2_inventory`, `llc_v2_inspect`).

### 2. Reconnaissance (read-only; paste everything)

```bash
wr_llc_v2_inventory $OUT 2>&1 | tee ~/llc_v2_inventory.txt | tail -30
ls $F1/SST.*.data | wc -l                       # folder spans 174 h -> expect 174
ls $F1 | grep -E "^SS|^STDOUT" | head -8
grep -iE "deltaT|nIter0" $F1/STDOUT.0000 | head -6
ls -l $MASKDIR
ls -l $GRIDDIR | head -40
```

What to expect: the inventory's `data:[...]` column should list `SST,SSS,SSU,SSV,Eta,...` for every folder and the summary should read `with SST.*.data: <all folders> (2023-01-01 01:00 to 2024-02-08 ...)`. Each `hFac*.bits` should be 5,246,467,200 bytes (173 levels × 30,326,400). 2D grid files (`XC.data`, `Depth.data`, ...) should be 970,444,800 bytes; `hFacC.data` 173 times that; `RC.data` 692 bytes. If sizes differ, tell me — the grid step then needs adapting.

>I received this: PermissionError: [Errno 13] Permission denied: '/nobackupp27/dbwhitt/llc_4320/OUT/2024_01_31_230000_to_2024_02_08_200000';  the rest checked out ok.

**Fixed (2026-09-10).** That folder cannot be listed by you (permission denied; it is the newest segment, presumably still being written or not yet opened up). The inventory, discovery, dry run and full run now skip any folder they cannot list, log `cannot list folder (Permission denied); skipped`, and report them: the inventory summary gains a line `unreadable (permission denied), skipped: N: <folders>` and the extraction summary a count `unreadable_folders=N`. Please `git pull`, rerun the inventory, and this time paste the actual outputs rather than "checked out ok" — I need the `tail -30` of the inventory and the two `ls -l` listings (mask and grid directories) to confirm the byte sizes the grid step relies on:

```bash
wr_llc_v2_inventory $OUT 2>&1 | tee ~/llc_v2_inventory.txt | tail -30
ls -l $MASKDIR
ls -l $GRIDDIR | head -40
ls -ld $OUT/2024_01_31_230000_to_2024_02_08_200000   # who owns it / its mode
```

Question: is `2024_01_31_230000_to_2024_02_08_200000` the last folder in `$OUT`? If so it is just the in-progress segment and we ignore it until it opens up; if there are readable folders after it, tell me.

>Answer -- see the output below

```
(ocean14) pleiades> wr_llc_v2_inventory $OUT 2>&1 | tee ~/llc_v2_inventory.txt | tail -30

  SIheffF.data       3
  SIhsalt.data       83
  SIhsnow.data       83
  SIuice.data       83
  SIvice.data       83
  SSS.data       83
  SST.data       83
  SSU.data       83
  SSV.data       83
  Salt.bloat      10
  Salt.shrunk     34
  SaltF.data       3
  SaltFF.data       1
  Theta.bloat      10
  Theta.shrunk     35
  ThetaF.data       2
  ThetaFF.data       1
  U.bloat      10
  U.shrunk     34
  V.bloat      10
  V.bloat.bad  1
  V.shrunk     34
  W.level1     2
  W.shrunk     34
  oceFWflx.data       83
  oceQnet.data       83
  oceQsw.data       83
  oceSflux.data       83
  oceTAUX.data       83
  oceTAUY.data       83
(ocean14) pleiades> ls -l $MASKDIR
total 20991528
-rw-r--r-- 1 bcnelson vistech  262306148 May 28 17:23 allOffsets_4320.c
-r--r--r-- 1 bcnelson vistech 5246467200 Apr  3 15:40 hFacC.bits
-r--r--r-- 1 bcnelson vistech   30326400 Apr 23 16:35 hFacM.bits
-r--r--r-- 1 bcnelson vistech 5246467200 Apr  3 15:40 hFacS.bits
-r--r--r-- 1 bcnelson vistech 5246467200 Apr  3 15:41 hFacW.bits
-rw-r--r-- 1 bcnelson vistech   87299989 May 28 17:16 offsets.C
-rw-r--r-- 1 bcnelson vistech     408018 May 28 17:17 offsets.M
-rw-r--r-- 1 bcnelson vistech   87299042 May 28 17:17 offsets.S
-rw-r--r-- 1 bcnelson vistech   87299099 May 28 17:18 offsets.W
(ocean14) pleiades> ls -l $GRIDDIR | head -40
total 524617316
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:41 AngleCS.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:44 AngleSN.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 18:00 BEDMACHINE_on_LLC4320_v16.bin
-rw-r--r-- 1 dbwhitt s2620    970444800 May  6 14:43 Depth.data
-rwxr--r-- 1 dbwhitt s2620          696 Mar 31 17:55 DRC.data
-rwxr--r-- 1 dbwhitt s2620          149 Mar 31 17:55 DRC.meta
-rwxr--r-- 1 dbwhitt s2620          692 Mar 31 17:51 DRF.data
-rwxr--r-- 1 dbwhitt s2620          149 Mar 31 17:51 DRF.meta
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:44 DXC.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:44 DXF.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:45 DXG.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:45 DXV.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:49 DYC.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:52 DYF.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:53 DYG.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:53 DYU.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 18:00 GEBCO2025_on_LLC4320_v16b.bin
-rw-r--r-- 1 dbwhitt s1353 167886950400 Mar 31 18:11 hFacC.data
-rw-r--r-- 1 dbwhitt s1353          228 Mar 31 18:11 hFacC.meta
-rw-r--r-- 1 dbwhitt s1353 167886950400 Mar 31 18:16 hFacS.data
-rw-r--r-- 1 dbwhitt s1353          228 Mar 31 18:16 hFacS.meta
-rw-r--r-- 1 dbwhitt s1353 167886950400 Mar 31 18:13 hFacW.data
-rw-r--r-- 1 dbwhitt s1353          228 Mar 31 18:13 hFacW.meta
-rw-r--r-- 1 dbwhitt s2620    970444800 Apr 14 12:18 maxhFacC_2D.bin
-rw-r--r-- 1 dbwhitt s2620          692 Mar 31 18:31 PRef4EOS.data
-rw-r--r-- 1 dbwhitt s2620          149 Mar 31 18:32 PRef4EOS.meta
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:53 RAC.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:53 RAS.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:53 RAW.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:54 RAZ.data
-rwxr--r-- 1 dbwhitt s2620          692 Mar 31 17:50 RC.data
-rwxr--r-- 1 dbwhitt s2620          149 Mar 31 17:50 RC.meta
-rw------- 1 dbwhitt s2620          251 Jun  4 14:15 README
-rwxr--r-- 1 dbwhitt s2620          696 Mar 31 17:57 RF.data
-rwxr--r-- 1 dbwhitt s2620          149 Mar 31 17:57 RF.meta
-rw-r--r-- 1 dbwhitt s2620          692 Mar 31 18:31 RhoRef.data
-rw-r--r-- 1 dbwhitt s2620          149 Mar 31 18:32 RhoRef.meta
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:54 XC.data
-rw-r--r-- 1 dbwhitt s2620    970444800 Mar 31 17:55 XG.data
(ocean14) pleiades> ls -ld $OUT/2024_01_31_230000_to_2024_02_08_200000 
lrwxrwxrwx 1 dbwhitt s2620 69 Sep  3 08:56 /nobackupp27/dbwhitt/llc_4320/OUT/2024_01_31_230000_to_2024_02_08_200000 -> /nobackupp27/dbwhitt/llc_4320/MITgcm/run_sal_90x90x19493/run_sep32026
```

### 3. Network and credentials

```bash
curl -sI --max-time 15 https://s3-west.nrp-nautilus.io | head -1
mkdir -p ~/.aws && chmod 700 ~/.aws && touch ~/.aws/credentials && chmod 600 ~/.aws/credentials
```

Put the `llc4320-v2` key pair into `~/.aws/credentials`:

```
[default]
aws_access_key_id = ...
aws_secret_access_key = ...
```

Then (expect `[]` for an empty bucket; an error means credentials or endpoint):

```bash
python -c "from wrangler.ogcm import llc_v2; fs = llc_v2.s3_filesystem(asynchronous=False); print(fs.ls('llc4320-v2'))"
```

If `curl` hangs, outbound HTTPS is blocked on the PFE: stop and tell me (we would write to `$SCR` and push with `mc mirror`, see step 6).

### 4. Dry run, then the first hour to scratch

```bash
wr_llc_v2_surface $OUT /tmp/unused --dry-run -v 2>&1 | tee ~/llc_v2_dryrun.log
grep -c "would write" ~/llc_v2_dryrun.log        # = hours on disk; ~9,700 for Jan 2023 - Feb 2024
grep -E "WARNING|numbering convention" ~/llc_v2_dryrun.log | sed 's/^.*INFO //; s/^.*WARNING /WARNING /' | cut -c1-160 | sort | uniq -c | sort -rn | head -20
tail -2 ~/llc_v2_dryrun.log
```

>This crashes with: PermissionError: [Errno 13] Permission denied: '/nobackupp27/dbwhitt/llc_4320/OUT/2023_06_04_060000_to_2023_06_09_180000/data'  But I can see there is no data in that data/ folder and the SST files are above it anyhow.  I think you need to modify your script accordingly.

**Fixed (2026-09-10).** In that folder `data` is a directory (elsewhere it is the MITgcm namelist file) and it is unreadable to you; the namelist reader tripped over it. The `data`/`STDOUT` parameter readers, the folder listing, the per-hour field reads and the grid-file reads now all catch permission errors, log a warning and continue (a folder without readable parameters simply gets no dating cross-check; an unreadable field file is treated as missing and the store is left incomplete for a later re-run). Please `git pull` and rerun the dry run; the summary-grep now also counts the `cannot list`/`cannot read` warnings:

```bash
wr_llc_v2_surface $OUT /tmp/unused --dry-run -v 2>&1 | tee ~/llc_v2_dryrun.log
grep -c "would write" ~/llc_v2_dryrun.log
grep -E "WARNING|numbering convention" ~/llc_v2_dryrun.log | sed 's/^.*INFO //; s/^.*WARNING /WARNING /' | cut -c1-160 | sort | uniq -c | sort -rn | head -25
tail -3 ~/llc_v2_dryrun.log
ls -la $OUT/2023_06_04_060000_to_2023_06_09_180000 | grep -vE "\.(data|meta)$" | head -20
```

The last line shows what else that odd folder holds (STDOUT? a namelist under another name?) so the dating cross-check can still work there; not critical.

Every folder should log `numbering convention: absolute` (or `relative-to-nIter0`) and there should be no `disagrees` or `spans ... but holds` warnings. A folder that warns is one whose file count does not match its span; paste those lines.

> Here is what I see:
```
(ocean14) pleiades> tail -3 ~/llc_v2_dryrun.log
2026-09-11 05:14:01,605 INFO would write /tmp/unused/20240131T23.zarr  <- SST.0001091700.data  [SST]
discovered=9503 written=0 skipped=0 incomplete=0 unreadable_folders=1 model_dt_s=[5.0, 8.0, 12.0, 18.0, 20.0]
  unreadable, skipped: 2024_01_31_230000_to_2024_02_08_200000
```

```bash
mkdir -p $SCR
wr_llc_v2_surface $OUT $SCR --limit 1 --mask-dir $MASKDIR --grid-dir $GRIDDIR -v 2>&1 | tee ~/llc_v2_first.log
ls $SCR
wr_llc_v2_inspect $SCR/20230101T01.zarr --eta $F1/Eta.0000000720.data --png ~/llc_v2_sst_first.png
wr_llc_v2_inspect $SCR/grid.zarr --var Depth
wr_llc_v2_inspect $SCR/grid.zarr --var XC
```

Expect: the store is `20230101T01.zarr` (first hour = 01:00 UTC, `selected_iteration: 720`); `value check: OK` and `mask check: OK` for Theta (the mask check compares the SST land mask with Eta's zeros); `grid.zarr` attributes list `variables` written and `skipped` files; Depth per-face max a few thousand metres, XC per-face min/max within ±180. Look at the PNG: 13 faces, continents where they belong, warm tropics, cold poles. Peak memory is ~3 GB (a 970 MB field plus masks); if the login node kills it, run steps 4–5 in an interactive PBS session.

### 5. First store to S3, then stop

```bash
wr_llc_v2_surface $OUT s3://llc4320-v2/SURFACE --limit 1 --mask-dir $MASKDIR --grid-dir $GRIDDIR -v
wr_llc_v2_inspect s3://llc4320-v2/SURFACE/20230101T01.zarr
```

Paste the outputs of steps 2–5 and wait for my review before step 6.

### 6. Full run

Pick the fields (`SURFACE_FIELDS` in `llc_v2.py` lists all known prefixes): SST only is `--fields SST`; everything surface is e.g. `--fields SST,SSS,SSU,SSV,Eta,oceQnet,oceQsw,oceFWflx,oceTAUX,oceTAUY,KPPhbl,PhiBot`. Each field adds ~1 GB read and ~0.45 GB written per hour, i.e. roughly 4 TB per field per year of output.

Option A — directly to S3 from the login node (simplest; NAS limits long PFE processes, so it may get killed and need restarting, which is safe):

```bash
nohup wr_llc_v2_surface $OUT s3://llc4320-v2/SURFACE --fields SST --mask-dir $MASKDIR --grid-dir $GRIDDIR > ~/llc_v2_full.log 2>&1 &
grep -c " wrote " ~/llc_v2_full.log; grep -c WARNING ~/llc_v2_full.log; tail -3 ~/llc_v2_full.log
```

Option B — PBS job writing to nobackup, then push with `mc` (use if the PFE kills long runs or compute nodes lack outbound network):

```bash
# in the PBS script: wr_llc_v2_surface $OUT $SCR/SURFACE --fields SST --mask-dir $MASKDIR --grid-dir $GRIDDIR
# afterwards, on a PFE:
module load S3/mc
mc alias set nautilus https://s3-west.nrp-nautilus.io <access key> <secret key>
mc mirror $SCR/SURFACE nautilus/llc4320-v2/SURFACE
```

Either way the run is restartable: complete stores are skipped, partial ones rewritten, and newly landed folders are picked up on the next invocation.

## Q&A

**Claude (2026-08-28):** Before writing extraction code, some questions raised by comparing `ExtractFields.m` / `dimitris_notes_v2.txt` against the existing (separate) S3-transfer pipeline in the sibling repo `llc4320-native-grid-preprocessing`:

1. **Which simulation is this?** Is the raw output at `/nobackupp27/dbwhitt/llc_4320/OUT` on Pleiades a *different, newer* LLC4320 run from the one already Zarr-ified and served from `s3://dbof` by `llc4320-native-grid-preprocessing` (source `/orcd/data/abodner/003/LLC4320/LLC4320` on MIT's ORCD cluster)? I'm assuming yes ("v2" = a distinct simulation, not a second copy of the existing one) — please confirm.
>A. Yes, v2 is a new run, not a second copy of the existing one.

2. **Regional or global?** `ExtractFields.m` extracts one lat/lon-windowed region (Gulf of Mexico) on faces 4/5 only, with face-specific rotation logic for vector fields (`AngleCS`/`AngleSN`, U/V, oceTAUX/oceTAUY — the `case {1,2}` vs `case {4,5}` branches). "Extract *all* of the data" suggests full-global, all 13 faces, no windowing — which means generalizing that per-face rotation logic beyond faces 4/5. Can you confirm global is the goal, not a set of named regions like the Gulf-of-Mexico example?
>A.  That is correct, we wish to go global

3. **Time range and cadence.** Dimitris' example spans 2023-03-27 to 2023-09-28. Is that the full extent of v2 output currently on Pleiades, or just what he needed for his own project? What's the actual available date range and output cadence, and is the run still producing new output?
>A. Good question.  I think there is at least one full year on disk now and they may create more.

4. **What counts as a "surface field"?** The six native 2D diagnostics (`KPPhbl, Eta, PhiBot, oceQnet, oceQsw, oceFWflx`) and the 2D vector pair (`oceTAUX/oceTAUY`) need no bitmask/offset decoding. `Theta` (SST), `Salt`, `W`, `U`, `V` are stored as full 3D fields requiring the `.shrunk` + bitmask reader — SST would just be their `k=0` slice. Since the goal is to start with SST: should the pipeline read full 3D `Theta` and slice `k=0`, or is there a lighter surface-only file for it that isn't mentioned in the notes? Same question applies to future "surface" additions (SSS, surface U/V) that are really 3D fields under the hood.
>A. Yes, we wish to extract the SST field with `k=0` slice.

5. **Reading the raw binary format in Python.** The `.shrunk` files need `read_llc_fkij`/`llc_shrunk_read` (MATLAB, using `offsets.idx` + `hFacC/hFacS/hFacW` bitmasks) or Dan's compiled `extract4320` binary — both MATLAB/compiled-only today. `xmitgcm` is already a dependency in `llc4320-native-grid-preprocessing`, but I don't see evidence it understands this shrunk/bitmask compression. Do we have (or know of) a Python-native reader for this format, or does the pipeline need to shell out to `extract4320`, or should we reimplement the offset/bitmask decoding directly in Python?
>A. I am not aware of a package.  Please try to find one.  If not, we will need to reimplement the offset/bitmask decoding directly in Python.

6. **Where does this code actually run?** The raw files live on NASA Pleiades (`/nobackupp27`, `/nobackupp28`, `/nobackupp29`), which this session can't reach. Is the plan for me to write extraction code now that you (or a PBS job) run directly on a Pleiades login/compute node, with a credentialed push to Nautilus S3 as a separate step? Do Pleiades compute nodes have outbound network access to `s3-west.nrp-nautilus.io`, or does the S3 upload need to happen from elsewhere after staging locally?
>A. Right, I will install this package on the Pleiades login node and run the extraction code there myself.

7. **Output layout/format on S3.** `llc4320-native-grid-preprocessing` documents a Zarr convention in `docs/Data_Organization.md` — a shared `grid.zarr` plus one `{YYYYMMDDTHH}.zarr` store per date. Should `llc4320-v2` mirror that layout (so existing readers like `dbof.llc4320_ingestion.get_llc_timestep_data` could work against it with minor changes), or should it more literally mirror `ExtractFields.m`'s own output layout (flat per-field-per-date binary files)?
>A. Yes, let's use a Zarr layout.  What is your recommendation for chunking?

8. **Where should the new code live?** `wrangler/ogcm/llc_v2.py` already exists on this branch as an empty stub — is that the intended home (parallel to `llc.py`, which backs the existing per-timestamp NetCDF pipeline used by `extract/ex_ogcm.py`)? Or should the raw-Pleiades-to-S3 step live in `llc4320-native-grid-preprocessing` instead, next to its existing `dbof.transfer` code, since that's conceptually the closer analog?
>A. Yes, let's use `llc_v2.py` for the extraction code.

9. **Scale.** "All of the data" globally at native LLC4320 resolution (4320×4320×13 faces) for the full v2 time range is a large volume, and full-depth 3D fields (51 levels) would be much larger still. Should an initial pass be scoped to the native 2D fields + SST (and later SSS) globally, with full 3D/depth extraction treated as a later phase? Any sense of total volume, or storage/egress constraints on the `llc4320-v2` bucket?
>A. As it will be good to test the code, let's start with SST only to scope out the full extent

10. **Bucket setup.** `docs/nautilus/s3_DBOF.md` documents the one-time `aws s3api create-bucket` + policy steps used to stand up `dbof`. Has `llc4320-v2` already been created on `s3-west.nrp-nautilus.io`, or is that a step you'll run yourself (per the wrangler house rule that you run all commands that touch shared/external state)?
>A. I have created the new bucket on `s3-west.nrp-nautilus.io`.  It is named `llc4320-v2`.

**Claude (2026-08-31):** Thanks — that resolves 1, 2, 3, 6, 8, 9, and 10. Following up on 5 and 7, plus two new questions raised by trying to act on your answers:

11. **(Follow-up to 5) Searched for an existing Python reader — didn't find one for this exact format, but found something close.** No public package reads NASA's specific `.shrunk` + `.bits` + `offsets.idx` on-disk layout — it looks like in-house tooling (Bron Nelson / Dan's `extract4320`, Kaitlin Zhang's `matlab_v0`), not published anywhere I could find. However, `xmitgcm.llcreader` (already a dependency of `llc4320-native-grid-preprocessing`) implements the *same underlying trick* for ECCO's compressed LLC4320 distribution: wet-point-only data is unpacked back onto the full grid with a 4-line boolean-mask fill (`xmitgcm/llcreader/llcmodel.py::_decompress`), against per-point masks named `mask_c`/`mask_w`/`mask_s` — which lines up suspiciously well with `hFacC.bits`/`hFacS.bits`/`hFacW.bits` in `ExtractFields.m`. So the decompression *math* is a known, trivial, already-proven pattern — the hard part is just parsing NASA's specific file layout (the `.bits` bitmask byte format and what `offsets.idx`/`off_C`/`off_S`/`off_W` actually encode), which I don't have a spec for. `ExtractFields.m` only *calls* `read_llc_fkij`/`llc_shrunk_read` — their definitions live in `/nobackup/kzhang/llc_4320/extract/matlab_v0` on Pleiades, which wasn't part of what you shared. Two ways forward, not mutually exclusive:
    - **(a)** Could you get me those two functions' MATLAB source (or point me to someone who can explain the `.bits`/`.idx` byte layout)? That's what I'd need to write a faithful pure-Python decoder.
    - **(b)** As a stopgap so we're not blocked on (a): for the first SST-only pass, the Python pipeline could shell out to the already-compiled `extract4320` binary (same one `ExtractFields.m`'s "generate commands" section already drives) to do the raw decode, and have Python own everything downstream — assembling the global grid, writing Zarr, pushing to `llc4320-v2`. We'd swap in a pure-Python reader later without changing anything downstream. Want me to proceed this way for the first pass?

    One thing that simplifies this for SST specifically: `Theta` is a scalar, cell-center field, so none of `ExtractFields.m`'s face-rotation logic (the `case {1,2}` vs `case {4,5}` branches, only needed for vector fields like U/V and the wind stresses) applies. We can defer solving that generalization-to-13-faces problem until we add a vector field.

>A. Yes, proceed with (b).  Unfortunately, I cannot view into the `/nobackup/kzhang/llc_4320/extract/` folder. 

12. **(Follow-up to 7) Chunking recommendation.** Since we're mirroring the Zarr convention `llc4320-native-grid-preprocessing` already uses, I'd recommend matching its established on-disk chunk shape for surface stores — `{"face": 1, "j": 720, "i": 720}` (native LLC tile size, one face per chunk; no `k` since SST is already sliced to a 2D `k=0` field). That keeps `llc4320-v2` dimensionally consistent with `dbof`'s `llc_surf_timestep_chunks` convention, in case any tooling ever wants to read both. Does that work, or is there a reason to chunk differently (e.g. combining all 13 faces into one chunk for small SST-only files)?
>A. Yes, that works.

13. **Nautilus credentials on the Pleiades login node.** Since you're running this yourself there: how should the code get `llc4320-v2` write credentials — the `ENDPOINT_URL` env var + boto3's default credential chain (the pattern `wrangler/s3_io.py` already uses), or something else you'd rather standardize on?
>A. See what we've done in the PAB repository for Nautilus and try to mimic that: `Oceanography/python/PAB/`.  I expect that I will need to copy credentials onto the Pleiades login node. 

14. **Date discovery.** `ExtractFields.m` discovers available dates dynamically by scanning `dir('*/U*.shrunk')` and parsing timestamps out of the folder names, rather than taking a hardcoded date list. Given you're not sure of the exact extent on disk and it may grow, I'm planning to keep that same auto-discovery approach (scan-and-extract-whatever's-there) rather than hardcoding dates, so re-running the pipeline naturally picks up new output. Flag me if you'd rather it work off an explicit date list instead.
>A. Yes, that is good.

**Claude (2026-09-01):** Checked `$OS_OGCM/LLC_v2/extract/matlab_v0/` — yes, this is exactly what question 11 needed. `llc_shrunk_mex.c` (the MEX gateway) contains the full, precise decompression algorithm in C, and it's simple enough that I ported it straight to pure Python/NumPy: `wrangler/ogcm/llc_v2.py`. No MATLAB, no compiled binary, no shelling out to `extract4320` needed after all — question 11 is resolved in favor of option (a), and better than expected.

What the format actually is, for the record: each `hFacC/S/W.bits` mask file is a simple bit-per-gridpoint mask (LSB-first per byte, `13*FS*FS` bits per vertical level, concatenated across levels — NZ is just `filesize / bytes-per-level`); each `<field>.<10-digit timestep>.shrunk` file is the wet-point values only, packed as a stream of big-endian float32, in the same flat facet-major/row-major order as the mask's set bits. Decoding one level is genuinely just "read that level's mask bits, popcount them to know how many floats to read, unpack big-endian floats into the True positions of a zero-filled array, reshape to `(13, FS, FS)`" — the `(13, FS, FS)` result is already in the same native facet layout `xmitgcm`/`dbof` use (no lat/lon "map" reprojection needed, so I skipped porting the MEX's `twodify()`), which lines up nicely with the chunking we already agreed on in question 12.

Wrote and ran a synthetic round-trip test (`wrangler/tests/test_llc_v2.py`) since there's no real `.shrunk`/`.bits` sample data available locally to test against — it builds small fake mask+data files using the identical bit-packing scheme and checks decode correctness, including multi-level offset handling for when we eventually need `k>0` (U/V/W/Salt). All 6 tests pass in `ocean14`.

What's implemented now: `read_shrunk_field()`/`read_sst()` read one global field at one timestep, in-memory, as a `(13, FS, FS)` NumPy array. What's *not* yet built: the discovery/orchestration layer (looping over timesteps, assembling a Zarr store, pushing to `llc4320-v2`) — and while digging through your files I hit a new snag worth flagging before I wire that up:

15. **Timestep vs. calendar date.** The `.shrunk` filenames are keyed by MITgcm *iteration number* (e.g. `Theta.0000021240.shrunk`), not by date — and `matlab_v0/README.md`'s own example directory is named `2023_05_24_060000_to_2023_05_29_060000` (a multi-day range) containing presumably many per-timestep files inside. To drive extraction from a calendar date (or to auto-discover "what dates are available," per question 14) I need either: the iteration-to-date arithmetic for this run (a reference start date + seconds-per-iteration, so `iteration = (date - start) / dt`), or a real example directory listing from Pleiades (`ls /nobackupp27/dbwhitt/llc_4320/OUT` and the contents of one date-range folder) so I can infer the convention rather than guess it. Do you have either of those handy, or is this something Dimitris/Kaitlin Zhang would know?

>A. Here are a few of the folders in the `/nobackupp27/dbwhitt/llc_4320/OUT` directory:
>```
>2023_01_01_000000_to_2023_01_08_060000
>2023_01_08_060000_to_2023_01_12_020000
>...
>2024_01_30_100000_to_2024_01_31_230000
>```

**Claude (2026-09-03):** Thanks. Status first, then the next round of questions.

**Resolved and built this round** (all in `wrangler/ogcm/llc_v2.py`, tested in `wrangler/tests/test_llc_v2.py`, CLI at `bin/wr_llc_v2_sst`):

- **15 (iteration vs. date) — answered by Dimitris' own notes.** The loop quoted in `dimitris_notes_v2.txt` (and `ExtractFields.m` lines 13-28) is the date rule: *date = folder start time + n hours*, where n is the file's 0-based position in its folder's sorted listing. So output is hourly, the folder name anchors the calendar, and the iteration number in the filename is never converted — no `dt`/reference-date arithmetic needed. Implemented as `discover_timesteps()`, which scans `*/Theta.*.shrunk` (not `U*.shrunk`, so SST doesn't depend on U being present). Because the position rule silently shifts every later date if one file is missing, it also cross-checks each folder — constant iteration stride, and folder `_to_` end time consistent with the file count — and logs a warning on any mismatch. It also infers the model timestep from the stride (`infer_timestep_seconds`; v1 was 25 s = 144 iterations/hour) so we learn v2's `dt` for free on the first real run.
- **13 (credentials) — mimicked PAB.** Same pattern as `PAB/nautilus/s3_push.py` and `pab/report/publish.py::NautilusS3Backend`: endpoint from `ENDPOINT_URL` (default `https://s3-west.nrp-nautilus.io`), keys from the boto3 default chain (`AWS_PROFILE`, default profile, in `~/.aws/credentials`, or `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`), path-style addressing (required by Nautilus' Ceph RGW). On Pleiades you'll need a `~/.aws/credentials` (`chmod 600`) with a `[default]` section holding a key pair that can write to `llc4320-v2`.
- **Zarr output, dbof layout.** One `{YYYYMMDDTHH}.zarr` per hour containing `Theta` as `(face, j, i)`, chunked `(1, 720, 720)` per Q12, float32, NaN over land; store attributes `selected_iteration`, `selected_date_utc`, `source_folder`, `source_file`; index coordinates `face`/`j`/`i`; plus a `grid.zarr` holding `maskC` (the k=0 wet mask from `hFacC.bits`). Writes are face-by-face with read-back verification, and each store is marked `complete` only at the end, so the pipeline is idempotent: rerunning skips complete stores, rewrites interrupted ones, and picks up newly landed folders (PAB's `s3_push.py` behaviour). Destination can be `s3://llc4320-v2/SURFACE` or a local directory (useful for a first look at real data).
- **CLI:** `wr_llc_v2_sst <OUT dir> <maskDir> <dest> [--dry-run] [--limit N] [--start YYYY-MM-DD[THH]] [--end ...] [--no-grid] [--endpoint] [--profile]`.

**Questions:**

16. **One real folder listing, please.** From Pleiades: `ls /nobackupp27/dbwhitt/llc_4320/OUT/2023_01_01_000000_to_2023_01_08_060000 | head -40` and `ls .../2023_01_01_000000_to_2023_01_08_060000/Theta.*.shrunk | wc -l`. That folder spans 174 h, so this settles (a) hourly cadence, (b) 174 vs 175 files — i.e. whether the `_to_` end hour is exclusive or also present in the next folder (if inclusive I need to de-duplicate the boundary hour; the code currently warns), (c) the iteration stride and hence v2's `dt`, (d) that filenames are 10-digit iterations like the README example. Equivalent: run `wr_llc_v2_sst <OUT> <maskDir> /tmp/x --dry-run` there and send me the log.

>A. Here is the output of the first command:
>```
> data
> data.cal
> data.exch2
> data.exch2_36x36x113847
> data.exch2_72x72x29271
> data.exch2_90x90x19492
> data.exf
> data_init
> data.kpp
> data.pkg
> data.pkg_init
> data.salt_plume
> data.seaice
> data.shelfice
> data.tides
> Eta.0000000720.data
> Eta.0000001440.data
> Eta.0000002160.data
> Eta.0000002880.data
> Eta.0000003600.data
> Eta.0000004320.data
> Eta.0000005040.data
> Eta.0000005760.data
> Eta.0000006480.data
> Eta.0000007200.data
> Eta.0000007920.data
> Eta.0000008640.data
> Eta.0000009360.data
> Eta.0000010080.data
> Eta.0000010800.data
> Eta.0000011520.data
> Eta.0000012240.data
> Eta.0000012960.data
> Eta.0000013680.data
> Eta.0000014400.data
> Eta.0000015120.data
> Eta.0000015840.data
> Eta.0000016560.data
> Eta.0000017280.data
> Eta.0000018000.data
>```

The second command gave: "No such file or directory".

17. **Where does `hFacC.bits` live on Pleiades?** `ExtractFields.m` reads `maskDir`, but that variable is set in the matlab_v0 setup, not in the file you shared. The only mask path it contains is Bron's `/nobackupp29/bcnelson/MITgcm/compressedOutput/work/bitmask.4320x173` (used by `extract4320`). Its name suggests FS=4320 × 173 levels; if `ls -l` shows a size that's a multiple of 30,326,400 bytes (= 13·4320²/8, one level's mask) then it's the same format and my reader can use it directly with `detect_nz`. Could you `ls -l` that file and locate the `hFacC.bits`/`hFacS.bits`/`hFacW.bits` directory?

18. **Bucket layout.** I've defaulted to `s3://llc4320-v2/SURFACE/{YYYYMMDDTHH}.zarr` + `s3://llc4320-v2/SURFACE/grid.zarr` — dbof's `LLC4320_RAW/SURFACE` layout minus the `LLC4320_RAW/` level, since this bucket is single-purpose. OK, or would you rather keep the extra level for symmetry with `dbof`?

19. **What goes in `grid.zarr`?** Is v2 on the identical LLC4320 horizontal grid as v1? If yes, the simplest path is to copy `XC`, `YC`, `rA`, `Depth`, etc. from `s3://dbof/LLC4320_RAW/SURFACE/grid.zarr`. If not, is there a v2 `gridDir` on Pleiades with `XC.data`, `YC.data`, `Depth.data` (plain big-endian float32 MITgcm files — easy to add a reader for)? For now `grid.zarr` holds only `maskC`.

20. **Volume and quota.** `wrangler/scripts/llc_v2_sst_volume.py` estimates the SST-only pass at ~0.45 GB per hourly store after Zarr's default zstd compression (assuming 70 % wet points and 1.5× compression on ocean values): ~11 GB/day, **~4 TB per year of output**, 8,760 stores and ~4.1 million objects per year. Does `llc4320-v2` have a quota that accommodates this (and the later fields)? If 4 TB/yr is too much for SST alone, the cheap levers are a coarser time cadence (e.g. every 3 or 6 hours via a `--stride` option) or a stronger codec; say which you'd prefer, if any.

21. **Outbound network from the login node.** Before the first real run, please check `curl -sI https://s3-west.nrp-nautilus.io | head -1` on the PFE. If it hangs, outbound HTTPS is blocked and we'll need to write locally and push from a host that can reach Nautilus. The Pleiades environment also needs `pip install numpy zarr s3fs` (zarr ≥ 3; numpy alone is enough for the decoder).

22. **Suggested first-run sequence** (no question, just the plan): `--dry-run` → `--limit 1 --no-grid` to a *local* directory, then eyeball the field (`-2 < SST < 35 °C`, NaN where land should be, continents in the right place per face) → `--limit 1` to S3 → full run under `nohup`/`screen` or PBS. Each hour reads ~0.68 GB and writes ~0.45 GB, so a full year is ~4 TB of upload — roughly a day at 50 MB/s, longer on a slower link. I'd like to see the first real decoded field before the full run in case the mask/data pairing differs from the synthetic tests.

Note: this round ran as Fable 5.1 rather than Opus 5 (no way to switch models within the session).

**Claude (2026-09-05):** Read the partial answer to 16. Three things it tells me, one thing it doesn't, and what I did about each; the concrete next steps are in the new **On Pleiades** section above.

- **Each folder carries the run's MITgcm namelists** (`data`, `data.cal`, `data.diagnostics`, ...). That's better than a listing: `deltaT` and `nIter0` in `data` let a file be dated from its iteration number, independently of Dimitris' position rule. `llc_v2.py` now parses them (`read_data_namelist`) and `discover_timesteps` cross-checks the two datings per folder, logging a warning if they disagree (dates still follow the position rule until we decide otherwise). Step 3 on Pleiades asks for the namelist values directly.
- **`Eta.*.data` files step by 720 iterations and start at 720, not 0.** So either `deltaT` = 5 s with hourly output, or 25 s with 5-hourly output — and if `nIter0` = 0, the first file is 1 h after the segment start, which would make the "start + n hours" rule one hour off. The namelist (step 3) and the Eta file count (step 2: ~174 if hourly) settle this.
- **The 2D fields are plain `.data`**, as `ExtractFields.m` implied. Added `read_data_field` (big-endian float32, full 13·FS² grid per level) — needed for Eta/KPPhbl/etc. later anyway, and used now by the new inspector to validate the SST land mask against Eta's zeros.
- **The `Theta.*.shrunk` command failed**, but almost certainly because the `...` in my shorthand was pasted literally rather than because the files are missing (`ExtractFields.m` builds `Theta.<iter>.shrunk` names from `U*.shrunk`, so they should sit next to Eta). Step 2 uses an explicit `$F1` path and lists every field/extension present with counts.

New tool for the first real decode, since I can't look at Pleiades: `wr_llc_v2_inspect <store> [--eta Eta.<iter>.data] [--png out.png]` prints per-face statistics, flags implausible values and stray exact zeros, checks the land mask against Eta, and can save a 13-panel image. Questions 17-21 stay open; steps 4-5 on Pleiades produce their answers, so no new numbered questions this round. Model note: Fable 5.1 again, not Opus 5.

**Claude (2026-09-05, prompt 6):** Step 2 failed with the explicit path, so the first folder really has no `Theta.<iteration>.shrunk` files; the typo theory is dead. What we still lack is the folder's actual contents beyond the first 40 alphabetical entries (`data*`, `Eta.*.data`). Rather than guess names, I wrote `wr_llc_v2_inventory` (see the revised step 2 in **On Pleiades**): one line per folder with the fields present as `.shrunk` and as `.data`, the iteration range and any other entries, a summary of which folders hold `Theta.*.shrunk` and their date range, and an optional bounded search for `*.shrunk` under another root (Bron Nelson's `compressedOutput` tree is the obvious candidate). The three candidate explanations, and how the output distinguishes them, are written out there. My bet is on the first — `ExtractFields.m` discarding everything before 2023-03-27 smells like "the shrunk 3D output begins then" — in which case the pipeline already does the right thing and the SST record just starts later than January 2023.

23. **Could you ask Dimitris (or Kaitlin) directly** whether the compressed 3D output (`U/V/Theta/Salt/W .shrunk`) exists for all of 2023-01 onward or only from some date, and whether it lives in `/nobackupp27/dbwhitt/llc_4320/OUT` or under Bron's `compressedOutput`? One sentence from them would short-circuit the detective work.

Model note: Fable 5.1 again, not Opus 5.

## Logging

The "Logs" section will record Claude's work.  Please use the following format:

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

...

## Logs

### 2026-08-28 (Examined LLC4320 v2 raw-extraction context; opened discussion in Q&A)

Read the prompt's listed context (`wrangler/ogcm/ExtractFields.m`, `wrangler/ogcm/dimitris_notes_v2.txt`) plus the existing code they feed into (`wrangler/extract/ex_ogcm.py`, `wrangler/preproc/pp_ogcm.py`, `wrangler/ogcm/llc.py`), and — per the "Related Repositories" pointer in this repo's `CLAUDE.md` — the sibling repo `llc4320-native-grid-preprocessing`, specifically its S3-transfer pipeline (`src/dbof/transfer/{pipeline,config,zarr_io}.py`, `configs/transfer/run_surface.yaml`), `src/dbof/llc4320_ingestion/get_raw_data.py`, and `docs/Data_Organization.md` / `docs/nautilus/s3_DBOF.md`.

Key findings that shaped the questions:

- `ExtractFields.m` extracts one lat/lon-windowed *region* (Gulf of Mexico, faces 4/5 only) from raw, compressed (`.shrunk`) MITgcm binary output on NASA Pleiades (`/nobackupp27/dbwhitt/llc_4320/OUT`), using face-specific rotation logic for vector fields and a bitmask+offsets reader (`read_llc_fkij`/`llc_shrunk_read`) or a compiled extraction binary (`extract4320`) — both MATLAB/compiled-only; found no Python equivalent in either repo.
- `llc4320-native-grid-preprocessing`'s existing `dbof.transfer` pipeline is a different thing: it copies an *already Zarr-ified* LLC4320 store (source `/orcd/data/abodner/003/LLC4320/LLC4320` on MIT's ORCD cluster) into `s3://dbof/LLC4320_RAW/{SURFACE,DEPTH}` on Nautilus (`s3-west.nrp-nautilus.io`). It never touches raw Pleiades binary output, so it's a useful convention reference (Zarr store layout, S3 storage-options patterns) but not reusable code for this task.
- `wrangler/ogcm/llc_v2.py` already exists on this branch as an empty stub — a plausible home for the new module.
- `wrangler/s3_io.py` is a separate, simpler boto3/smart_open-based S3 helper (not Zarr-aware), already pointed at a Nautilus endpoint.
- Found no existing references anywhere to a bucket named `llc4320-v2`, no `xmitgcm` support for the `.shrunk`/bitmask format, and no evidence this environment has access to Pleiades.

Given the raw-format-reader gap, the regional-vs-global ambiguity, and the "start with SST" ambiguity (SST lives inside the 3D `Theta` output, not the six native 2D diagnostic fields), wrote 10 clarifying questions into the `## Q&A` section above before writing any extraction code, per prompt #1's instruction to discuss first.

Note: the prompt asked to use Opus 5; this session ran as Sonnet 5, which has no way to switch models mid-conversation, so this was flagged to Xavier directly rather than silently ignored.

### 2026-08-31 (Read Q&A answers, researched Python readers for the shrunk format, continued discussion)

Read Xavier's answers to all 10 questions from the previous round. Confirmed: v2 is a distinct, newer LLC4320 simulation; scope is global (all 13 faces), not regional; at least a year of raw output is on Pleiades disk already (possibly more coming); SST = `k=0` slice of the 3D `Theta` field; output should be Zarr-formatted on `s3://llc4320-v2` (already created by Xavier on `s3-west.nrp-nautilus.io`); the new code lives in `wrangler/ogcm/llc_v2.py`; Xavier will install the package and run extraction himself on the Pleiades login node; and the first pass is deliberately scoped to SST only, globally, to scope out the full time extent before adding more fields.

Per question 5's ask to "try to find" an existing Python package before reimplementing the offset/bitmask decoder: ran several web searches for a Python reader of NASA's `.shrunk`/`.bits`/`offsets.idx` compressed LLC output. Found no public package that reads this exact on-disk format — it appears to be in-house tooling (Bron Nelson's `extract4320`, Kaitlin Zhang's `matlab_v0` MATLAB routines) that was never published. However, found that `xmitgcm.llcreader.llcmodel` (in `xmitgcm`, already a dependency of `llc4320-native-grid-preprocessing`) implements the identical underlying compression trick for ECCO's own distribution of compressed LLC4320 output: a `_decompress(data, mask, dtype)` helper that fills a NaN-initialized array at boolean-mask-true positions, using masks named `mask_c`/`mask_w`/`mask_s` loaded from a zarr group — strongly analogous to `hFacC.bits`/`hFacS.bits`/`hFacW.bits` in `ExtractFields.m`. Read `xmitgcm`'s `utils.py`, `mds_store.py`, and `llcreader/llcmodel.py` (via WebFetch) to confirm this. Conclusion relayed to Xavier: the decompression math is a known, trivial, already-proven pattern (reusable once we have real inputs), but the actual byte-level format of NASA's `.bits` mask files and `offsets.idx` isn't documented anywhere I could find, and the MATLAB functions that decode them (`read_llc_fkij`, `llc_shrunk_read`) are only *called*, not *defined*, in `ExtractFields.m` — their source lives in `/nobackup/kzhang/llc_4320/extract/matlab_v0` on Pleiades, which hasn't been shared. Asked for that source (or someone who can explain the format), and separately proposed a stopgap: shell out to the already-compiled `extract4320` binary for the first SST-only pass (Python would own discovery, grid assembly, Zarr writing, and the S3 push), swapping in a pure-Python decoder later without touching anything downstream. Also noted that SST specifically sidesteps `ExtractFields.m`'s face-rotation logic entirely, since that's only needed for vector fields.

Also used this round to answer Xavier's chunking question (recommended `{"face": 1, "j": 720, "i": 720}`, matching `llc4320-native-grid-preprocessing`'s existing `llc_surf_timestep_chunks` convention for surface stores, with `k` dropped since SST is pre-sliced to 2D) and raised two new questions: how S3 credentials should be supplied on the Pleiades login node, and confirming the plan to auto-discover available dates by scanning the raw-output directory (mirroring `ExtractFields.m`'s `dir('*/U*.shrunk')` approach) rather than working off a hardcoded date list. Wrote all of this into the `## Q&A` section (items 11-14) rather than writing extraction code yet, since Xavier's prompt #2 asked to continue the discussion, not implement.

### 2026-09-01 (Found the raw-format reader source; ported it to Python; wrote and passed tests)

Xavier downloaded files from Dimitris into `$OS_OGCM/LLC_v2/extract/` (env var resolves locally to `/Users/xavier/Projects/Oceanography/data/OGCM/LLC_v2/extract/`) and pointed at `matlab_v0/` as the likely answer to question 11's ask for the missing reader source. Checked: it is. `matlab_v0/` contains `llc_shrunk_read.m`, `llc_shrunk_read_field.m`, `llc_shrunk_read_facets.m`, `llc_shrunk_precompute.m`, `llc_shrunk_setup.m`, a `README.md`, and — critically — `llc_shrunk_mex.c`, the actual C implementation the MATLAB wrappers all just dispatch to via a compiled MEX gateway. (`extract/dan_v0/` also had the alternative C++ `extract4320`/`generateOffsets` tool plus a 200MB `offsets.idx`, but wasn't needed once the MEX source made the format fully clear.)

Read `llc_shrunk_mex.c` in full. The format is: mask files (`hFacC/S/W.bits`) are a flat bit-per-gridpoint mask, LSB-first per byte, `13*FS*FS` bits per vertical level, levels concatenated back-to-back (so `NZ = filesize / bytes-per-level`); `<field>.<10-digit iteration>.shrunk` files hold only the *wet*-point values, as a stream of big-endian float32, in the same flat facet-major/row-major order as the mask's set bits — i.e. classic run-length-free "boolean mask compression," matching what I'd found `xmitgcm.llcreader` implements conceptually for ECCO's distribution (question 11's earlier finding). Decoding one level requires no MATLAB-specific machinery: read that level's mask bits, popcount them for the read length, unpack big-endian floats into the `True` positions of a zero-filled array, reshape to `(13, FS, FS)`. The MEX's `'facets'` output mode (as opposed to its `'map'` mode, which also runs a `twodify()` reprojection into a single lat/lon-like raster) returns exactly that native `(13, FS, FS)` layout — no facet-rotation logic involved at all, and it's already dimensionally what `xmitgcm`/`dbof` expect, so that's the layout this port targets; `twodify()` was intentionally not ported.

Ported the algorithm to pure Python/NumPy in `wrangler/ogcm/llc_v2.py`: `mask_file_for_field`, `mask_bytes_per_level`, `detect_nz`, `read_mask_level`, `level_byte_offset` (cumulative popcount across levels, for future `k>0` fields — trivially 0 for the surface), `decompress_level`, `read_shrunk_field`, and a `read_sst` convenience wrapper. Deliberately dependency-light (just `os`+`numpy`) so it doesn't drag in the rest of `wrangler`'s heavier stack (astropy_healpix, cartopy) onto the Pleiades login node Xavier will run this from.

Since there's no real `.shrunk`/`.bits` sample data available locally to validate against (it only exists on Pleiades), wrote `wrangler/tests/test_llc_v2.py`: builds small synthetic mask+data files using the identical bit-packing/big-endian-float32 scheme (random masks, random values) and checks exact round-trip decode, including a 3-level case that exercises `level_byte_offset`'s cumulative-popcount logic, plus the field->mask filename mapping and missing-file error handling. Ran `conda run -n ocean14 python -m pytest wrangler/tests/test_llc_v2.py -v` — all 6 pass.

What's still missing before this is a runnable end-to-end pipeline: discovering what timesteps/dates are actually on disk, assembling a per-date Zarr store, and the S3 push — deliberately left for a follow-up round once questions 13 (S3 credentials on the Pleiades login node) and a new one (15: how MITgcm iteration numbers map to calendar dates, since `.shrunk` filenames are keyed by iteration, not date, and I don't have the run's reference-date/timestep-length or a real Pleiades directory listing to infer it from) are answered. Logged question 15 in the `## Q&A` section.

### 2026-09-03 (Read answers to 13-15; built discovery, Zarr/S3 output, and the SST pipeline; tests pass)

Read Xavier's answers to questions 13-15. For 13, examined the PAB repo (`/home/xavier/Oceanography/python/PAB`): the Nautilus pattern there is `nautilus/s3_push.py` (boto3 client with `endpoint_url` from `ENDPOINT_URL`, defaulting to `https://s3-west.nrp-nautilus.io`, and a `profile_name` from `AWS_PROFILE`, defaulting to `default`, read from `~/.aws/credentials`; idempotent uploads that skip objects already present with the same size) and `pab/report/publish.py::NautilusS3Backend` (same, plus `addressing_style: path`, which Nautilus' Ceph RGW requires; the Kubernetes jobs mount the `prp-s3-credentials` secret at `/root/.aws/credentials`). Also re-read `llc4320-native-grid-preprocessing`'s `src/dbof/io/filesystems.py`, `src/dbof/transfer/zarr_io.py` and `pipeline.py` for the S3-Zarr write conventions (fsspec s3 filesystem with `asynchronous=True` wrapped in `zarr.storage.FsspecStore`; `create_array(..., dimension_names=('face','j','i'))`, NaN fill, per-tile read-back verification; store names `YYYYMMDDTHH.zarr`, attrs `selected_iteration`/`selected_date_utc`).

For 15, the folder names Xavier listed (`2023_01_01_000000_to_2023_01_08_060000`, ..., `2024_01_30_100000_to_2024_01_31_230000`) plus the loop quoted in `dimitris_notes_v2.txt` settle the date rule: Dimitris never converts iteration numbers; he assigns each file in a folder the folder's start time plus its 0-based position (in sorted order) in hours -- i.e. hourly output anchored by the folder name. Adjacent folders share a boundary time in their names, so whether the end hour is exclusive (likely, else it would appear in two folders) is still to be confirmed against a real listing (asked as question 16).

Implemented, appended to `wrangler/ogcm/llc_v2.py` (the decoder from 2026-09-01 is unchanged except that `decompress_level`/`read_shrunk_field`/`read_sst` gained a `dry_value` argument so the pipeline can write NaN over land while the default 0.0 still matches the C reference):

- Discovery: `parse_folder_name`, `Timestep` dataclass, `discover_timesteps(out_dir, field, start, end)` implementing the position-based date rule with two consistency checks per folder (constant iteration stride; folder span vs. file count, tolerating end-exclusive or end-inclusive) and a global duplicate-date check, all as logged warnings; `infer_timestep_seconds` derives the model `dt` from the within-folder stride.
- Output: `store_name_for_date`, `s3_endpoint`, `s3_filesystem` (PAB credential conventions + dbof's botocore config), `open_zarr_group` (local path or `s3://`), `store_is_complete` (a `complete=True` attribute is written only after all variables are verified, so interrupted stores are redone), `write_surface_variable` (face-by-face writes with read-back verification, `(1,720,720)` chunks clipped for small test grids), `write_grid_store` (`grid.zarr` with `maskC` from `hFacC.bits` -- horizontal grid variables deferred pending question 19), `write_timestep_store`, and `extract_sst` (discover -> skip complete -> decode with NaN land -> write; `dry_run`, `limit`, date window).
- CLI: `wrangler/scripts/llc_v2_sst.py` (`parser()`/`main(args)`, matching `inspect_h5.py`) and `bin/wr_llc_v2_sst`.
- `wrangler/scripts/llc_v2_sst_volume.py`: the storage estimate quoted in question 20 (~0.45 GB/hour, ~4 TB/year for SST alone).

Tests: added 9 tests to `wrangler/tests/test_llc_v2.py` -- folder-name parsing, store naming, discovery dates/ordering/date-window, gap warnings (a folder with a missing hour triggers both the stride and span warnings and defeats `dt` inference), NaN dry values, an end-to-end local `extract_sst` run (dry run writes nothing; 5 hourly stores + `grid.zarr` with correct names, dims, chunks, attrs, values and NaN land; second run skips all; a store with `complete=False` is rewritten; `--limit`), s3fs settings (endpoint/profile from env vs. explicit args; `profile` is forwarded to `AioSession`), and the CLI parser + dry run. `conda run -n ocean14 python -m pytest wrangler/tests/test_llc_v2.py`: 14 passed, 1 skipped (the end-to-end test needs `zarr`, which `ocean14` lacks). The same file in the `dbof` env (zarr 3.1.5): 15 passed.

Notes: the `$OS_OGCM/LLC_v2/extract/matlab_v0` files consulted on 2026-09-01 live on Xavier's Mac; on this Linux machine `$OS_OGCM` (`/home/xavier/Oceanography/data/OGCM/`) has no `LLC_v2/`, so the format was not re-checked -- the 2026-09-01 port and its round-trip tests stand. Also closed an unterminated code fence in Xavier's answer to question 15 so the rest of the file renders. Wrote questions 16-22 into the Q&A (real folder listing to confirm cadence/end-exclusivity/stride; location of `hFacC.bits` and whether Bron's `bitmask.4320x173` is the same format; bucket layout; `grid.zarr` contents; volume/quota; outbound network from the PFE; suggested first-run sequence). This session ran as Fable 5.1, not Opus 5.

### 2026-09-05 (Prompt 5: wrote the "On Pleiades" hand-off; added namelist/.data readers and an inspection CLI)

Xavier is logged into Pleiades with the repo installed and will not install Claude there, so prompt 5 asks for a self-contained "On Pleiades" section. Read his partial answer to question 16 first: `ls | head -40` of `2023_01_01_000000_to_2023_01_08_060000` shows MITgcm namelist files (`data`, `data.cal`, `data.exch2*`, `data.exf`, `data.kpp`, `data.seaice`, `data.shelfice`, `data.tides`, ...) followed by `Eta.<iteration>.data` files at iterations 720, 1440, ..., i.e. a stride of 720 starting at 720; the `Theta.*.shrunk | wc -l` command returned "No such file or directory", most likely because the literal `...` in my shorthand path was pasted (question 16 as written). Two consequences: (1) the model timestep is either 5 s (hourly output) or 25 s (5-hourly), and if `nIter0` = 0 the first file sits at +1 h from the segment start, which would put Dimitris' "start + n hours" rule off by one hour -- the namelist in each folder can settle this; (2) the 2D diagnostics are plain `.data` files, matching `ExtractFields.m`'s `read_llc_fkij` usage.

Code added to `wrangler/ogcm/llc_v2.py`: `read_data_field` (plain MITgcm big-endian float32 `.data`, one level of `13*FS*FS`, size-validated); `read_data_namelist` (tolerant regex parse of `deltaT`, `nIter0`, `startTime`, `nTimeSteps`, `endTime`, `dumpFreq`, `taveFreq` from a folder's `data`, handling several assignments per line, `D` exponents, `#` comments); and `_check_against_namelist`, called by `discover_timesteps` per folder, which logs the iteration*deltaT-implied offset of the first file and file spacing and warns if they disagree with the position rule (dates are not changed). New CLI `wrangler/scripts/llc_v2_inspect.py` + `bin/wr_llc_v2_inspect`: opens a local or S3 store, prints attrs, per-face wet fraction/min/max/mean, flags values outside [-3, 40] degC and exact zeros among wet points, optionally cross-checks the land mask against an `Eta.<iter>.data` file (fractions of Eta-wet points that are NaN in SST and vice versa -- both ~0 if the mask bit order and pairing are right), and optionally writes a 13-panel PNG if matplotlib is present.

Tests: 4 added (`.data` round trip with wrong-FS/level errors; namelist parsing incl. multi-assignment lines; discovery warning when the namelist disagrees and silence when it agrees; the inspector end-to-end on a local store with a matching Eta -> OK and a shuffled Eta -> MISMATCH). `ocean14`: 17 passed, 2 skipped (zarr); `dbof`: 19 passed.

Wrote the "On Pleiades" section as an 8-step checklist with copy-pasteable commands and what to paste back: environment check; one-folder inventory (`sed`-collapsed field/extension counts, Theta/Eta counts, folder count and range); namelist greps for `deltaT`/`nIter0`/`dumpFreq` plus `data.cal` and `data.diagnostics`; mask-file search (Bron's `bitmask.4320x173`, `find` for `*.bits`, with the 30,326,400-bytes-per-level size test); `curl` to the Nautilus endpoint, `~/.aws/credentials` setup and an `fs.ls('llc4320-v2')` check; `--dry-run` with the warning greps; first decode to nobackup scratch with `--limit 1 --no-grid` and inspection against Eta with a PNG; first store to S3 with `--limit 1`; then stop and report before the full `nohup` run. Q&A entry for 2026-09-05 summarises the above; questions 17-21 remain open and are answered by steps 4-5. Smoke-testing the `bin/` scripts locally showed `python bin/wr_llc_v2_sst` fails with `ModuleNotFoundError: wrangler` unless the package is pip-installed (neither `ocean14` nor `dbof` has it installed here; `bin/` rather than the repo root lands on `sys.path`), so both script modules gained an `if __name__ == '__main__'` block and step 1's fallback now says `python -m wrangler.scripts.llc_v2_sst ...` / `pip install -e .`. Verified `python -m` for both scripts and the expected clean `OSError: Not a directory` on a bogus `OUT` path. This session ran as Fable 5.1, not Opus 5.

### 2026-09-05 (Prompt 6: step 2 on Pleiades found no Theta.*.shrunk files; added an inventory tool and revised the step)

Xavier reported that step 2 of the Pleiades checklist fails: with `$F1` set explicitly to `/nobackupp27/dbwhitt/llc_4320/OUT/2023_01_01_000000_to_2023_01_08_060000`, `ls $F1/Theta.*.shrunk` finds nothing (step 1 passed: `wrangler`, `zarr`, `s3fs` import and both `bin/` scripts are on PATH). That rules out last round's "literal `...`" explanation; the folder genuinely has no `Theta.<iteration>.shrunk` files. The output of the first step-2 command (the `sed`-collapsed field/extension inventory) was not pasted, and it is the one that would show what the folder does hold beyond the 40 alphabetically-first entries seen so far (`data*` namelists and `Eta.*.data`).

Three explanations, ranked: (1) the compressed 3D output begins later than 2023-01-01 -- `ExtractFields.m` discards everything before 2023-03-27, which may be precisely because earlier segments have only 2D `.data` diagnostics; if so the pipeline already behaves correctly (folders without Theta are skipped) and the SST record simply starts later; (2) the 3D fields live elsewhere, most plausibly under Bron Nelson's `/nobackupp29/bcnelson/MITgcm/compressedOutput/` tree (where his `bitmask.4320x173` is), with `OUT` holding only 2D output -- the pipeline would then take a separate root for the shrunk files; (3) a different naming convention (`T.<iter>.shrunk`, `Theta.<iter>.data.shrunk`, lower case, a `shrunk/` subfolder).

To settle it without further ad-hoc `ls` round trips, added to `wrangler/ogcm/llc_v2.py`: `OUTPUT_FILE_RE` (`<field>.<6-12 digit iteration>.<ext>`), `FolderInventory` dataclass, `inventory_folder`, `inventory(out_dir, max_folders)`, `summarize_inventory(invs, field, ext)` (folder count, `(field, ext) -> number of folders` union, folders with/without `Theta.*.shrunk` and their date range), and `find_files(root, suffix, max_depth, limit)` (a bounded `find -maxdepth`, skipping unreadable directories; fixed its depth convention after the first test run counted first-level-subfolder files at depth 1). New CLI `wrangler/scripts/llc_v2_inventory.py` + `bin/wr_llc_v2_inventory`: one line per folder (`files`, iteration range, `shrunk:[...]`, `data:[...]`, `other:` entries), the summary, and `--find ROOT --depth N` for `*.shrunk` elsewhere. One new test covers the inventory, summary, `find_files` depth semantics and the CLI output; `ocean14`: 18 passed, 2 skipped (zarr); `dbof`: 20 passed.

Revised step 2 in the "On Pleiades" section: run `wr_llc_v2_inventory $OUT | tee ~/llc_v2_inventory.txt`, the `--find` search under Bron's tree, `ls -d` of `compressedOutput/*` and `/nobackupp27/dbwhitt/llc_4320/*`, then the remaining Eta/folder counts; explained which output distinguishes the three explanations; noted that steps 3-5 are independent of Theta and can proceed while 6-8 wait. Q&A: added a prompt-6 entry and question 23 (ask Dimitris/Kaitlin directly where and from when the shrunk 3D output exists). This session ran as Fable 5.1, not Opus 5.

### 2026-09-09 (Input from Dan et al.: surface fields are uncompressed .data; dating rule corrected; wrote Next Steps)

Read Dan Whitt's answers (he owns the run: `/nobackupp27/dbwhitt`), pasted by Xavier into `wrangler/ogcm/questions.txt`, plus his key note in the prompt. Findings, and what each changes:

- All global surface fields exist uncompressed in the OUT folders as `SST/SSS/SSU/SSV.<iteration>.data` (surface Theta/Salt/U/V) in MITgcm compact format, 4320 x 56160 big-endian real*4 = the `(13, 4320, 4320)` layout `read_data_field` (added 2026-09-05) already reads. So SST -- and every other surface field -- needs no shrunk decoding, no bitmask and no offsets. The shrunk decoder is kept for the later 3D phase. The missing `Theta.*.shrunk` in step 2 is explained: the Jan-Jun 2023 3D shrunk output was moved to the NAS export2 object store (`mc ls bdt-llc4320-v2/bdt-llc4320-v2/llc_4320_v2` shows `OUT/`, `bitmask.4320x173/`, `grid/`), which needs `mc`, not POSIX, and an access request to support@nas.nasa.gov.
- Dating: hourly snapshots; the first file in a folder is one hour after the folder start; the last is at the folder end; no duplicated hours. So the correct rule is `date = start + (n+1) h`, not the `start + n h` loop from `ExtractFields.m`/`dimitris_notes_v2.txt` that `discover_timesteps` implements -- the current code dates every file one hour early. `deltaT` is 5-20 s and `nIter0` varies per folder (both in `STDOUT.0*`), so iteration numbers alone cannot date files across folders. Dan also has a manifest relating SST files to date/time; asked Xavier for it as the basis of an exact test.
- Masks: `/nobackupp29/bcnelson/MITgcm/compressedOutput/work/bitmask.4320x173/` is a directory of `hFac*.bits` with 173 levels (Dan assumes same content as Kaitlin's). Format assumptions (flat big-endian float32 wet points, LSB-first bits, no headers; offsets only for sub-regions) confirmed as "Dan thinks so".
- Grid: horizontal grid identical to v1, but Depth, land mask, hFac and vertical grid differ; v2 grid files at `/nobackupp27/dbwhitt/llc_4320/grid_90x90x19493`. `grid.zarr` must use v2's own Depth/hFacC/maskC.
- Coverage: Jan 1 2023 - Feb 8 2024 on disk, still running (~9,700 hourly stores per field).

Per the prompt ("let me know how you wish to proceed in the Next Steps section"), wrote a plan rather than code: (1) retarget the pipeline at the uncompressed surface `.data` files with a field table (SST->Theta etc.), land->NaN from hFacC k=0 or exact zeros, same Zarr conventions, SSU/SSV as native components; (2) fix the dating rule and make the folder check strict, parse `STDOUT.0000` as well as `data`, add a manifest-based test/validation once the manifest is available; (3) build `grid.zarr` from `grid_90x90x19493` (size-validated 2D compact files, hFacC level 0, maskC); (4) rewrite the On Pleiades checklist for the new path; (5) small set of `ls`/`grep` outputs for Xavier to paste (grid dir, bitmask dir, `SS*`/`STDOUT` files, deltaT/nIter0), plus the manifest; (6) request export2 access in parallel for the depth phase; (7) decision: SST-only vs all surface fields in one sweep. Marked the existing On Pleiades checklist as superseded (steps 1 and 5 still valid). No code changed this round; the dating bug is documented above and fixed in plan item 2. This session ran as Fable 5.1, not Opus 5.

### 2026-09-09 (Prompt 2 under "Input from Dan et al.": implemented the plan; rewrote the Pleiades checklist)

Xavier approved the Next Steps plan. No listings or manifest had been pasted yet and no manifest file exists locally, so everything was built to validate its inputs by size and degrade gracefully.

`wrangler/ogcm/llc_v2.py` (now ~1,370 lines): module docstring rewritten around the two raw formats. **Dating fixed** to `date = folder start + (n+1) h` per Dan (was `start + n h`, one hour early); `_check_folder_consistency` now requires `nfiles == span hours` exactly. **Run parameters**: `_parse_params`, `read_stdout_params` (first `STDOUT.0000`/`STDOUT.*`, first 4 MB, both MITgcm echo forms), `read_run_params` (`data` first, STDOUT fills gaps, records the source); `_check_against_namelist` accepts absolute (`(iter0 - nIter0)*deltaT = 1 h`) or nIter0-relative (`iter0*deltaT = 1 h`) numbering, logs which matched, warns if neither. **Discovery** generalised: `discover_timesteps(out_dir, field='SST', ext='data', ...)` using `OUTPUT_FILE_RE`; `infer_timestep_seconds` returns the sorted list of distinct deltaT values (they vary per folder). **Field table** `SURFACE_FIELDS` (prefix -> dbof/xmitgcm variable name + attrs: SST->Theta, SSS->Salt, SSU->U, SSV->V, Eta, KPPhbl, PhiBot, oceQnet, oceQsw, oceFWflx, oceSflux, oceTAUX/Y, SIarea, SIheff, SIhsnow, SIuice/vice), `ZERO_IS_NOT_LAND` for sea-ice fields, `field_variable`, `surface_wet_mask` (hFacW/S/C per variable), `apply_land_mask` (mask, else exact zeros -> NaN). **Grid store** `write_grid_store(dest, grid_dir, mask_dir, ...)`: `GRID_2D_FILES` (MITgcm name -> xmitgcm name, e.g. RAC->rA, AngleCS->CS) written when the `.data` file has exactly one compact level; `hFacC/S/W.data` level 0 as `<name>_k0`; `RC/RF/DRC/DRF` as 1D `k`/`k_p1` arrays; `maskC` from `hFacC.bits`, else `hFacC.data` level 0 > 0, else `Depth > 0`; missing/odd-sized files recorded in a `skipped` attribute. **Stores**: `write_timestep_store(dest, step, {var: (array, attrs)}, complete=...)`, `store_is_complete` takes several variables. **Pipeline** `extract_surface(out_dir, dest, fields, mask_dir, grid_dir, ...)`: discovery driven by the first field, all requested fields into one store per hour, per-variable wet masks cached, a store missing a requested field is written but left `complete=False` (counted as `incomplete`) so a re-run revisits it; `extract_sst` is a thin wrapper. CLI: new `wrangler/scripts/llc_v2_surface.py` + `bin/wr_llc_v2_surface` (`--fields`, `--mask-dir`, `--grid-dir`, ...); `llc_v2_sst.py` is an alias; inventory defaults changed to `--field SST --ext data`; the inspector applies the SST plausibility verdict only to `Theta` (grid variables have 0, not NaN, over land).

Tests rewritten around a new synthetic tree of uncompressed hourly `.data` files with two segments of different deltaT/nIter0 (5 s/0 and 10 s/2160; a `data` namelist in one, a `STDOUT.0000` in the other), land = exact zeros with matching `hFac*.bits`: dating (hours 1-5, store names `T01..T05`, both numbering conventions, disagreement warning, gap warnings with the strict count), STDOUT/run-params parsing, end-to-end `extract_surface` (two variables per store, NaN land from mask and from zeros, idempotency, extra-field rewrite, incomplete stores, `--limit`, wrapper), `write_grid_store` from a synthetic grid dir (2D, 3-level hFacC, 1D RC/RF, wrong-size and missing files skipped, precedence), CLI parsing, inspector with the real synthetic Eta, inventory with explicit `--field Theta --ext shrunk`. `ocean14`: 19 passed, 3 skipped (zarr); `dbof`: 22 passed. `python -m` entry points verified for all four scripts.

Rewrote the "On Pleiades" section: env vars for OUT/F1/MASKDIR/GRIDDIR/SCR; (1) `git pull` + env check; (2) inventory, SST/STDOUT listings, mask and grid `ls -l` with expected byte sizes; (3) network + credentials; (4) dry run with a `uniq -c` summary of warnings/conventions, first hour to scratch with `--mask-dir/--grid-dir`, inspector on the SST store (vs `Eta.0000000720.data`, PNG) and on `grid.zarr` (`Depth`, `XC`); (5) first store to S3 then stop; (6) full run, Option A `nohup` to S3 from the PFE, Option B PBS job to nobackup + `mc mirror`. Added a status note under Next Steps; still wanted from Xavier: Dan's SST manifest and the SST-only vs all-fields decision (just `--fields`). This session ran as Fable 5.1 (as the prompt asked).

### 2026-09-10 (Prompt 3 under "Input from Dan et al.": permission errors on Pleiades; made all raw I/O tolerant)

Xavier ran the rewritten checklist and hit two crashes, both `PermissionError`: (step 2) `wr_llc_v2_inventory` on listing `$OUT/2024_01_31_230000_to_2024_02_08_200000` -- the newest segment, presumably still being written; (step 4) the dry run on `$OUT/2023_06_04_060000_to_2023_06_09_180000/data`, which in that folder is an unreadable *directory* rather than the MITgcm namelist file. He reported the other step-2 checks as "checked out ok" but pasted no listings, so the mask/grid byte sizes are still unconfirmed.

Changes in `wrangler/ogcm/llc_v2.py`: `read_data_namelist` and `read_stdout_params` catch `OSError` (and the directory-named-`data` case) and return `{}` with a warning; `discover_timesteps(..., unreadable=None)` and `inventory(..., unreadable=None)` skip folders that cannot be listed, warn `cannot list folder (...); skipped`, and append them to the caller's list; `extract_surface` collects them into `stats['unreadable_folders']` and treats an unreadable/undecodable field file as missing (store written but left incomplete); `write_grid_store` records unreadable grid files or `hFacC.bits` in the `skipped` attribute and falls back for `maskC`. CLIs: the inventory prints `unreadable (permission denied), skipped: N: ...` and returns them in the summary; the surface CLI prints `unreadable_folders=N` and lists them. New test `test_permission_errors_are_skipped` reproduces both failures with `chmod 000` (unlistable folder, unreadable `data/` directory and `STDOUT`, unreadable field file, unreadable grid file; skipped when running as root) and checks readers, discovery, inventory, dry-run stats, both CLIs' output, the incomplete-store path and the grid `skipped` attribute. Two exact-dict assertions in the end-to-end test and one in the inventory test were updated for the new keys. `ocean14`: 19 passed, 4 skipped (zarr); `dbof`: 23 passed.

In the On Pleiades section, added "Fixed (2026-09-10)" notes under Xavier's two reports with the commands to rerun after `git pull`, asked for the actual outputs this time (inventory tail, `ls -l` of mask and grid directories, `ls -ld` of the unreadable folder), asked whether the unreadable folder is the last one in `$OUT` (in-progress segment) and what else the odd `2023_06_04` folder contains. No change to the extraction logic or formats. This session ran as Fable 5.1, not Opus 5.
